<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      .grey-text{
        color: grey
      }
    </style>
  </head>

  <body>
    <h3>New message recieved from <span class="grey-text"><%= @contact.values[0]["email"] %></span></h3>
    <div class="pannel">
      <p> First Name: <%= @contact.values[0]["first_name"] %></p>
      <p> Last Name: <%= @contact.values[0]["last_name"] %></p>
      <p> Phone: <%= @contact.values[0]["phone"] %></p>
      <p>Attached Message is: <%= @contact.values[0]["message"] %><br></p>
    </div>
  </body>
</html>
